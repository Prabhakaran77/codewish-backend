spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/codewish
    username: postgres
    password: sa
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
